生成一期「东南亚卫星产业动态」日报并交付。\n\n【监测范围】国家（6 个）：泰国、印度尼西亚（印尼）、马来西亚、新加坡、柬埔寨、**越南**；领域（4 个）：卫星通信、导航（GNSS/北斗）、遥感（对地观测/SAR/光学）、**发射与卫星（含发射动态、地面站动态、卫星动态）**。\n\n【执行步骤】\n1. 用 WebSearch（topic=news）分别检索上述 6 国在四大领域的最新动态，每国至少 1 次检索（可合并领域关键词），力求覆盖通信/导航/遥感/发射。越南为新增监测国，须主动检索其卫星/航天动态（如 VNSC、VTOS、卫星计划、地面站、北斗/GNSS 合作、发射相关）。\n2. 整理为中文结构化 HTML 日报，严格遵循以下结构（顺序不可调整）：\n   - 顶部 `<header>`：标签 `ASEAN SAT. DAILY · 第 NNN 期`（NNN 取 manifest 下一期号，即现有最大期号+1，首期从 011 起），主标题「东南亚卫星产业动态」，meta 行含监测周期、6 国范围、4 大领域、出品「Harry 🎩 for 明华 · Astrolink BD」。\n   - 「本期核心看点」卡片区（4–6 张，含 🔴/🆕/🟡 等状态标记）。\n   - 「六国活跃度矩阵」表格：行=6 国，列=卫星通信/导航/遥感/发射，单元格用 ● 高 / ● 中 / ● 低(暂无) 标注热度。\n   - 按国别逐节（🇹🇭泰国 → 🇮🇩印度尼西亚 → 🇲🇾马来西亚 → 🇸🇬新加坡 → 🇰🇭柬埔寨 → 🇻🇳越南），每节下分 **四个子块**：`▎卫星通信` / `▎导航（GNSS/北斗）` / `▎遥感` / `▎发射与卫星（含发射动态·地面站·卫星动态）`。每条动态含：领域色条（卫星通信=蓝 / 导航=绿 / 遥感=粉 / 发射=橙 `--launch`）、标题、来源、日期、要点摘要（`<ul class=\"pts\">`）、💼 BD 关联建议（`.biz` 块，提及明华/Astrolink）。\n   - 「🧭 与明华 / Astrolink BD 的业务衔接建议」优先级列表（P0/P1/P2）。\n   - 底部 footer：期号、生成时间、出品署名（Astrolink，非 SmarCo）。\n3. **视觉风格（必须严格遵守）浅色调 + 移动端适配**：将以下 `:root` 变量原样写入 `<style>`（背景浅灰/白、文字深色），领域色：卫星通信蓝 `--com`、导航绿 `--nav`、遥感粉 `--rs`、发射橙 `--launch`；并在 `</style>` 前加入 `@media (max-width:600px)` 移动端规则（容器收窄、卡片单列、表格字号缩小、内边距减小，**仅针对手机宽度优化，不采用固定顶部 masthead 结构**）：\n   ```\n   :root{\n     --bg:#eef1f7; --panel:#ffffff; --panel2:#f3f6fc; --line:#dde3ef;\n     --txt:#1b2440; --sub:#62708f; --gold:#b07d12;\n     --com:#2c6fd6; --nav:#0f9d8c; --rs:#d6418f; --launch:#e8833a;\n     --hi:#e03131; --mid:#c08a16; --lo:#9aa6bd;\n   }\n   ```\n   移动端示例（可据版式微调）：\n   ```\n   @media (max-width:600px){\n     .wrap{max-width:100%;padding:16px 12px 40px;}\n     header{padding:22px 16px;}\n     h1{font-size:22px;}\n     h2{font-size:18px;margin:26px 0 12px;}\n     h3.country{font-size:16px;}\n     .cards{grid-template-columns:1fr;}\n     .card{padding:14px;}\n     table{font-size:12px;}\n     th,td{padding:8px 6px;}\n     .item .inner{padding:14px;}\n   }\n   ```\n   **对比度要求（浅色模式手机可读）**：所有正文、要点、来源、BD 关联块必须使用 `:root` 变量，严禁硬编码浅色文字（如 `#cfd9f2`、`#e8edf9` 等）。具体要求：`<body>` / `<p>` / `.pts` / `.biz` 普通文字用 `var(--txt)`；来源 `.src`、小字 `.sm`、副标题 `.sub-title` 用 `var(--sub)`；链接 `a` 用 `var(--com)` 并加下划线；`.biz b` 用 `var(--gold)`。\n   作为兜底，在 `</head>` 前必须再追加一个 `<style id=\"light-contrast-fix\">` 块，强制覆盖可能遗留的浅色文字：\n   ```html\n   <style id=\"light-contrast-fix\">\n   .pts,.pts li{color:var(--txt)!important;}\n   .biz{color:var(--txt)!important;background:rgba(176,125,18,.12)!important;border-left-color:var(--gold)!important;}\n   .biz b{color:var(--gold)!important;}\n   .src{color:var(--sub)!important;}\n   .sub-title{color:var(--sub)!important;}\n   .card .sm{color:var(--sub)!important;}\n   a{color:#1a4db5!important;text-decoration:underline!important;}\n   </style>\n   ```\n4. 文件名：`D:\\Harry的文件\\东南亚卫星产业动态_YYYY-MM-DD.html`（YYYY-MM-DD 用当天日期）。在 `<head>` 写入 `<meta name=\"archive-title\" content=\"一句话概括核心看点\">` 与 `<meta name=\"archive-tags\" content=\"泰国,印度尼西亚,马来西亚,新加坡,柬埔寨,越南,卫星通信,导航,北斗,遥感,发射\">`（标签取当期实际出现的关键词）。\n5. 若工作区有「双端文档同步」技能，加载并按其流程把该 HTML 同步到飞书 WorkPlace > work report 文件夹与 ima 知识库；否则跳过同步、仅本地保存。\n6. 最后用 present_files 交付该 HTML 文件。\n\n【注意】新闻时点以来源为准；重大合作需提示以官方与一线 BD 核实。保持简体中文。越南与「发射与卫星」板块若无可靠公开动态，如实标注「本期无新增公开动态（低热度）」，不得编造。SEA SATELLITE WEEKLY 一律改为 ASEAN SAT. DAILY；SmarCo 一律改为 Astrolink。移动端采用普通响应式收窄即可，不要使用固定顶部（masthead）+ 可滑动 body 的结构。